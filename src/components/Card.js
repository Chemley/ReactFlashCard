import React, { Component } from 'react';


class Card extends Component {
  let text = function text(text) {
    if (isFront === true)
      console.log("it's working!")
    } else {
      return state;
    }
}

  render () {
      <p className = "text"> {props.text}</p>
  }

};

export default Card;
